# Demo video

The screen recording lives at the repo root: **`regional-map-demo.mp4`**

It is embedded in the root `README.md` via an HTML `<video>` tag.

> **Note:** The file is ~580 MB. GitHub rejects files over 100 MB on a normal push — use [Git LFS](https://git-lfs.com/) or host the video externally (YouTube, Vimeo) if you publish this repo to GitHub.
